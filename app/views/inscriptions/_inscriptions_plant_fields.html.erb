<div class="row nested-fields">
  <div class="col-md-4">
    <%= f.input :plant_id, as: :select, collection: Plant.active.pluck(:name, :id).to_a, label: false, required: true %>
  </div>
  <div class="col-md-4">
    <%= f.input :quantity, label: false, required: true %>
  </div>
  <div class="col-md-2">
    <%= link_to_remove_association "x borrar planta", f, class: "btn btn-danger btn-mini" %>
  </div>
</div>
