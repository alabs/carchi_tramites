<dl>
  <% pending = query.by_event_type(event_type).pending.count %>
  <% approved = query.by_event_type(event_type).approved.count %>
  <% denied = query.by_event_type(event_type).denied.count %>
  <dt>Pendientes</dt>
  <dd><%= pending %></dt>
  <dt>Aprobados</dt>
  <dd><%= approved %></dt>
  <dt>Denegados</dt>
  <dd><%= denied %></dt>
</dl>

<%# column_chart [["Pendientes", pending], ["Aprobados", approved], ["Denegados", denied]] %>
